# AGENTS.md

このリポジトリで作業するAIエージェント向けの運用ルール。

## AWSへの変更は必ず1つずつ確認を取る

AWSアカウントに対する変更(リソース作成、IAMポリシーの追加、タスク定義の更新、サービスの再起動など)は、**1つの操作ごとに提案し、ユーザーの明示的な確認を得てから実行する**。複数の変更をまとめて連続実行しない。読み取り専用の確認コマンド(`describe-*`, `list-*`など)は対象外。

コンソール操作で学べる内容は、エージェントが代わりにCLIで実行せず、ユーザー自身がコンソールで操作する。エージェントは手順と「なぜそうするか」を説明する役に徹する。

**背景**: 2026-09-19、RDS接続作業でIAMポリシー追加からECSサービス更新・再起動テストまでを確認なしに一気に実行してしまい、ユーザーの学習機会を奪った。
